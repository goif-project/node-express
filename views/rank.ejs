<!DOCTYPE html>
<html>
  <head>
    <head>
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta name="format-detection" content="telephone=no">
    <title><%= title %></title>
    <script src="/js/jquery.js"></script>
    <link rel='stylesheet' href='/css/style.css' />
  </head>
  <body>
    <div class="wrapper_rank">
      <h1><img src="../img/T6.png" alt="HOME GOLF"></h1>
      <p class="rank_btn"><a href="../">TOPへ戻る</a></p>
      <ul class="top-rank-list">1~10位
        <% Rank1.forEach(function(scoreRank1) { %>
          <li class="main-list__item">
            <p class="board__title"><%= scoreRank1.user_name %></p>
            <p class="board__date"><%= scoreRank1.score %>点</p>
            </a>
          </li>
        <% }); %>
      </ul>
      <ul class="top-rank-list">11~50位
        <% Rank2.forEach(function(scoreRank2) { %>
          <li class="main-list__item">
            <p class="board__title"><%= scoreRank2.user_name %></p>
            <p class="board__date"><%= scoreRank2.score %>点</p>
            </a>
          </li>
        <% }); %>
      </ul>
      <p class="rank_btn"><a href="../">TOPへ戻る</a></p>
    </div>
  </body>
</html>
